// Fonte ÚNICA em @sintera/core (paridade Web↔Mobile). O DOMÍNIO do evento (health_events) — enums, tipos,
// mapeadores persistência↔domínio, predicados e seletores puros — vive uma vez só, no core. Este arquivo
// reexporta para preservar os import sites existentes ('@/lib/agenda/event').
export * from '@sintera/core'
