// Fonte ÚNICA em @sintera/core (paridade Web↔Mobile). Reexporta para preservar os import sites
// existentes ('@/lib/finance/expense'). A regra vive uma vez só, no core.
export { EXPENSE_DOC_TYPES, type ExpenseDocType, expenseDocLabel, isFiscalDocument } from '@sintera/core'
