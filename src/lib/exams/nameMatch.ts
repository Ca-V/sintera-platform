// Fonte ÚNICA em @sintera/core (paridade Web↔Mobile). Reexporta para preservar os import sites
// existentes ('@/lib/exams/nameMatch'). A regra vive uma vez só, no core.
export { type NameMatch, nameTokens, compareNames } from '@sintera/core'
