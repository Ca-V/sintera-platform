// Slot Timeline (Linha do tempo) — RESERVADO. Futuro: Activity Feed / itens recentes (incremento próprio). Sem domínio.
import { ReservedRegion } from './ReservedRegion'

export function TimelineSlot() {
  return <ReservedRegion title="Linha do tempo" />
}
