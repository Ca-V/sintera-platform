// Slot Quick Actions. Etapa 1: componente real, mínimo. Os pontos de entrada (SÓ navegação, sem regra de
// negócio nem priorização — MOBILE-014 §3.1) entram na Etapa 3.
import { View } from 'react-native'

export function QuickActionsSlot() {
  return <View />
}
