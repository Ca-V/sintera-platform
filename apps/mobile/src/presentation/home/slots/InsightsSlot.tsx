// Slot Insights — RESERVADO. Futuro: Insights Panel (incremento próprio). Sem lógica de domínio.
import { ReservedRegion } from './ReservedRegion'

export function InsightsSlot() {
  return <ReservedRegion title="Insights" />
}
