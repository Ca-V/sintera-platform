// Slot Summary (Resumo) — RESERVADO. Futuro: Daily Score / métricas-chave (incremento próprio). Sem domínio.
import { ReservedRegion } from './ReservedRegion'

export function SummarySlot() {
  return <ReservedRegion title="Resumo" />
}
